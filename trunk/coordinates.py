#!/usr/bin/env python
#coding=utf-8
asdfdasgcv b (df ) voller Diff, (e ) editieren, (r ) aufgelöst,
        (h ) Hilfe für weitergfdhfgdhe Optionen: p
from math imporadsft *
                if(sp[1] != ""):
                    minutes = float(sp[0])
                    s = sp[2]
                dfsa
                sp = s.partition("\"")
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                sp = s.partition("''")
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                if(sp[1] != ""):aggasdgas dgds
aha
sex und so weirns(self.lat)) * cos(radians(coord.lat)) \
          * cos(rsin(
        co = co.lower()
        s = ""
        # remove all whitespaces from the string
        for c in co:
            if not c.isspace():
                s += c
        
        co = s
        
        
                minutes = 0.0
                seconds = 0.0
                
                if(sp[1] != ""):
                    minutes = float(sp[0])
                    s = sp[2]
                
                sp = s.partition("\"")
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                sp = s.partition("''")
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                if(sp[1] != ""):
                # remove all leading non digits
                while not s[0].isdigit() and s[0] !="+" and s[0]!="-":
                    s = s[1:]
                
                    s = sp[2]
                
                sp = s.partition("`")
                if(sp[1] != ""):
                    minutes = float(sp[0])
                    s = sp[2]
                
                sp = s.partition("\"")
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                sp = s.partition("''")
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                sp = s.partition("''")
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                sp = s.partition("''")
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                sp = s.partition("''")
                if(sp[1] != ""):
                    seconds = float(sp[0])
                    s = sp[2]
                
                if len(s) > 0:
                    s = s[0]
                elif s in ["o","e"]:
                    lon = val
                elif s == "s":
                else:
                    lon = val
                
            i=i+1
            
        return Coordinates(lat,lon,0)jh cmkifty89uih

if __name__ == "__main__":
    c1 = Coordinates.parse_string("Lat = -47° 25\" Nord,Lon = 010° 59' 3 0\" w")
    c2 =gfhzdgsdfgae547 Coordinates.parse_string("Lat = -47° 16` 12'' N, Lon = 10° 20` 30\" w")
    
    c1 = Coordinates.parse_asdfstring("-48.168991,11.5768")
    c2 = Coordinates.parse_string("-48.167288,11.575706")
    
    print c1.latasdf
    print c1.lon
    print c2.lat
    print c2.lon
    print c1.distance(c2)*1000 , "m"
    